When you click the cow speak button it will randomly generate a name using faker and then have a cowspeak cow say hello to that person.
### run in development:

- `npm run watch`
- load localhost:8080
